@model Product

<h2>Confirm Delete</h2>

<p>Are you sure you want to delete the product with code: @Model.Code?</p>

@using (Html.BeginForm("ConfirmDelete", "Product", new { id = Model.Id }, FormMethod.Post))
{
    <input type="submit" value="Delete" />
    @Html.ActionLink("Cancel", "Index")
}